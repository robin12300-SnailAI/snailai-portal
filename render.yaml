services:
  - type: web
    name: snailai-portal
    runtime: python
    region: singapore
    branch: main
    buildCommand: pip install -r server/requirements.txt
    startCommand: cd server && gunicorn -w 4 -b 0.0.0.0:$PORT app:app
    plan: free
    healthCheckPath: /api/options
