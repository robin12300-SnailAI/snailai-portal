services:
  - type: web
    name: snailai-portal
    runtime: python
    region: singapore
    branch: main
    rootDir: server
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT app:app
    plan: free
    healthCheckPath: /
